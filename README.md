# GNU-MAKE READER

## Dependencies:
	glib & makeheaders (http://www.hwaci.com/sw/mkhdr/)(https://aur.archlinux.org/packages/makeheaders/)

## Compile with:

```shell
	make -j
```

## Test (& compile) with:

```shell
	make run -j
```
